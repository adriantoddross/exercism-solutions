/*
  Approaches
    1. Regex group/range to find letters a-z, case insensitive; return results of .match & .test to verify
    2. Run a for loop & map string chars to object (excluding punctuation), if length < 26, return false; ⚠️ will have issues with alphanumeric chars
*/

export const isPangram = () => {
  throw new Error("Remove this statement and implement this function");
};
